From f83f491e2256fc0bed48673438356e73bc7ac900 Mon Sep 17 00:00:00 2001
From: hexxa69 <johnyevsbryan.suico@cvsu.edu.ph>
Date: Sun, 26 Jul 2026 21:06:16 +0800
Subject: [PATCH] quotation automation

---
 .../Commands/SyncQuotationsFromOrders.php     |  66 +++++++++
 app/Models/Order.php                          |  18 +++
 app/Models/User.php                           |  28 ++--
 .../Models/SalesQuotation.php                 |  10 ++
 .../Observers/OrderItemObserver.php           |  33 +++++
 .../Observers/OrderObserver.php               |  34 +++++
 .../Services/AutoQuotationService.php         | 132 ++++++++++++++++++
 .../Models/UserSetting.php                    |  13 +-
 ...rce_order_id_to_sales_quotations_table.php |  31 ++++
 database/seeders/OrderSeeder.php              |  21 +++
 .../sales-order-management.css                |  47 ++++---
 .../sales-order-management.js                 |   2 +-
 12 files changed, 384 insertions(+), 51 deletions(-)
 create mode 100644 app/Console/Commands/SyncQuotationsFromOrders.php
 create mode 100644 app/Modules/SalesOrderManagement/Observers/OrderItemObserver.php
 create mode 100644 app/Modules/SalesOrderManagement/Observers/OrderObserver.php
 create mode 100644 app/Modules/SalesOrderManagement/Services/AutoQuotationService.php
 create mode 100644 database/migrations/2026_07_26_000001_add_source_order_id_to_sales_quotations_table.php

diff --git a/app/Console/Commands/SyncQuotationsFromOrders.php b/app/Console/Commands/SyncQuotationsFromOrders.php
new file mode 100644
index 0000000..b37aafd
--- /dev/null
+++ b/app/Console/Commands/SyncQuotationsFromOrders.php
@@ -0,0 +1,66 @@
+<?php
+
+namespace App\Console\Commands;
+
+use App\Models\Order;
+use App\Modules\SalesOrderManagement\Services\AutoQuotationService;
+use Illuminate\Console\Command;
+
+/**
+ * One-time backfill: generates/refreshes a Sales Quotation for every
+ * existing CRM order that doesn't have one yet (or whose items have
+ * changed since it was last synced).
+ *
+ * Going forward, new orders sync automatically via OrderObserver /
+ * OrderItemObserver — this command only exists to catch up orders that
+ * were seeded/placed before that automation was added, so the Sales
+ * Quotations screen has real, matching demo data to show.
+ *
+ * Usage: php artisan quotations:sync-from-orders
+ */
+class SyncQuotationsFromOrders extends Command
+{
+    protected $signature = 'quotations:sync-from-orders {--fresh : Re-sync every order, even ones that already have a quotation}';
+
+    protected $description = 'Backfill Sales Quotations from existing CRM orders (for demo/catch-up purposes)';
+
+    public function handle(AutoQuotationService $service): int
+    {
+        $query = Order::with('items')->orderBy('order_id');
+
+        if (! $this->option('fresh')) {
+            // Fast path: only touch orders that don't have a synced
+            // quotation yet, so re-running this doesn't redo work every time.
+            $query->whereDoesntHave('quotation');
+        }
+
+        $orders = $query->get();
+
+        if ($orders->isEmpty()) {
+            $this->warn('No CRM orders found — nothing to sync.');
+
+            return self::SUCCESS;
+        }
+
+        $created = 0;
+        $updated = 0;
+        $skipped = 0;
+
+        $this->withProgressBar($orders, function (Order $order) use ($service, &$created, &$updated, &$skipped) {
+            $quotation = $service->syncFromOrder($order);
+
+            if (! $quotation) {
+                $skipped++;
+            } elseif ($quotation->wasRecentlyCreated) {
+                $created++;
+            } else {
+                $updated++;
+            }
+        });
+
+        $this->newLine(2);
+        $this->info("Done. Created: {$created}, Updated: {$updated}, Skipped: {$skipped}.");
+
+        return self::SUCCESS;
+    }
+}
diff --git a/app/Models/Order.php b/app/Models/Order.php
index ceff8ff..a813c48 100644
--- a/app/Models/Order.php
+++ b/app/Models/Order.php
@@ -5,6 +5,7 @@
 use Illuminate\Database\Eloquent\Factories\HasFactory;
 use Illuminate\Database\Eloquent\Model;
 use Illuminate\Database\Eloquent\Relations\BelongsTo;
+use Illuminate\Database\Eloquent\Relations\HasMany;
 
 /**
  * SHARED / CORE MODEL — owned by the E-commerce module, not SCSM.
@@ -52,4 +53,21 @@ public function customer(): BelongsTo
     {
         return $this->belongsTo(Customer::class, 'customer_id', 'customer_id');
     }
+
+    /**
+     * Line items for this order (products, qty, unit price).
+     */
+    public function items(): HasMany
+    {
+        return $this->hasMany(OrderItem::class, 'order_id', 'order_id');
+    }
+
+    /**
+     * The Sales Quotation auto-generated from this CRM order, if any.
+     * See App\Modules\SalesOrderManagement\Services\AutoQuotationService.
+     */
+    public function quotation()
+    {
+        return $this->hasOne(\App\Modules\SalesOrderManagement\Models\SalesQuotation::class, 'source_order_id', 'order_id');
+    }
 }
diff --git a/app/Models/User.php b/app/Models/User.php
index 0d62409..0ca67c9 100644
--- a/app/Models/User.php
+++ b/app/Models/User.php
@@ -3,8 +3,6 @@
 namespace App\Models;
 
 // use Illuminate\Contracts\Auth\MustVerifyEmail;
-use App\Modules\SalesPerformanceReporting\Models\Region;
-use App\Modules\SalesPerformanceReporting\Models\UserSetting;
 use Database\Factories\UserFactory;
 use Illuminate\Database\Eloquent\Attributes\Fillable;
 use Illuminate\Database\Eloquent\Attributes\Hidden;
@@ -14,16 +12,13 @@
 use Illuminate\Foundation\Auth\User as Authenticatable;
 use Illuminate\Notifications\Notifiable;
 
-#[Fillable(['name', 'email', 'password', 'role', 'region_id', 'employee_code', 'department', 'avatar_initials'])]
+#[Fillable(['name', 'email', 'password'])]
 #[Hidden(['password', 'remember_token'])]
 class User extends Authenticatable
 {
     /** @use HasFactory<UserFactory> */
     use HasFactory, Notifiable;
 
-    public const ROLE_MANAGER = 'manager';
-    public const ROLE_EMPLOYEE = 'employee';
-
     /**
      * Get the attributes that should be cast.
      *
@@ -37,23 +32,20 @@ protected function casts(): array
         ];
     }
 
-    public function isManager(): bool
-    {
-        return $this->role === self::ROLE_MANAGER;
-    }
-
-    public function isEmployee(): bool
-    {
-        return $this->role === self::ROLE_EMPLOYEE;
-    }
-
+    /**
+     * SalesPerformanceReporting's region (users.region_id -> regions.id).
+     */
     public function region(): BelongsTo
     {
-        return $this->belongsTo(Region::class, 'region_id');
+        return $this->belongsTo(\App\Modules\SalesPerformanceReporting\Models\Region::class, 'region_id');
     }
 
+    /**
+     * SalesPerformanceReporting's per-user preferences (notifications,
+     * dark mode, quota reminders).
+     */
     public function settings(): HasOne
     {
-        return $this->hasOne(UserSetting::class);
+        return $this->hasOne(\App\Modules\SalesPerformanceReporting\Models\UserSetting::class, 'user_id');
     }
 }
diff --git a/app/Modules/SalesOrderManagement/Models/SalesQuotation.php b/app/Modules/SalesOrderManagement/Models/SalesQuotation.php
index 2496e6e..3b0fdf2 100644
--- a/app/Modules/SalesOrderManagement/Models/SalesQuotation.php
+++ b/app/Modules/SalesOrderManagement/Models/SalesQuotation.php
@@ -18,6 +18,7 @@ class SalesQuotation extends Model
 
     protected $fillable = [
         'customer_id',
+        'source_order_id',
         'tax_region_id',
         'quotation_date',
         'valid_until',
@@ -58,4 +59,13 @@ public function salesOrder()
     {
         return $this->hasOne(SalesOrder::class, 'quotation_id');
     }
+
+    /**
+     * The CRM/e-commerce order this quotation was auto-generated from, if
+     * any. Null for quotations a sales rep built by hand.
+     */
+    public function sourceOrder()
+    {
+        return $this->belongsTo(\App\Models\Order::class, 'source_order_id', 'order_id');
+    }
 }
diff --git a/app/Modules/SalesOrderManagement/Observers/OrderItemObserver.php b/app/Modules/SalesOrderManagement/Observers/OrderItemObserver.php
new file mode 100644
index 0000000..34f5233
--- /dev/null
+++ b/app/Modules/SalesOrderManagement/Observers/OrderItemObserver.php
@@ -0,0 +1,33 @@
+<?php
+
+namespace App\Modules\SalesOrderManagement\Observers;
+
+use App\Models\OrderItem;
+use App\Modules\SalesOrderManagement\Services\AutoQuotationService;
+use Illuminate\Support\Facades\DB;
+
+/**
+ * Order items are usually written one-by-one right after the order itself
+ * (see CustomerMockController::placeOrder()), outside of a single wrapping
+ * transaction. Re-running the sync on every item write means the quotation
+ * still ends up complete once the last item lands, without needing the
+ * whole request to be transactional.
+ */
+class OrderItemObserver
+{
+    public function saved(OrderItem $item): void
+    {
+        DB::afterCommit(function () use ($item) {
+            $order = $item->order()->first();
+
+            if ($order) {
+                app(AutoQuotationService::class)->syncFromOrder($order);
+            }
+        });
+    }
+
+    public function deleted(OrderItem $item): void
+    {
+        $this->saved($item);
+    }
+}
diff --git a/app/Modules/SalesOrderManagement/Observers/OrderObserver.php b/app/Modules/SalesOrderManagement/Observers/OrderObserver.php
new file mode 100644
index 0000000..3db059c
--- /dev/null
+++ b/app/Modules/SalesOrderManagement/Observers/OrderObserver.php
@@ -0,0 +1,34 @@
+<?php
+
+namespace App\Modules\SalesOrderManagement\Observers;
+
+use App\Models\Order;
+use App\Modules\SalesOrderManagement\Services\AutoQuotationService;
+use Illuminate\Support\Facades\DB;
+
+/**
+ * Watches the CRM/e-commerce `orders` table (App\Models\Order) and keeps
+ * SOM's Sales Quotations in sync automatically — no more manual
+ * "+ New Quotation" clicks for orders that already exist in the CRM.
+ */
+class OrderObserver
+{
+    public function created(Order $order): void
+    {
+        $this->sync($order);
+    }
+
+    public function updated(Order $order): void
+    {
+        $this->sync($order);
+    }
+
+    protected function sync(Order $order): void
+    {
+        // Wait for the surrounding transaction (order + its items) to
+        // actually commit before we try to price line items off it.
+        DB::afterCommit(function () use ($order) {
+            app(AutoQuotationService::class)->syncFromOrder($order->fresh());
+        });
+    }
+}
diff --git a/app/Modules/SalesOrderManagement/Services/AutoQuotationService.php b/app/Modules/SalesOrderManagement/Services/AutoQuotationService.php
new file mode 100644
index 0000000..1b8276e
--- /dev/null
+++ b/app/Modules/SalesOrderManagement/Services/AutoQuotationService.php
@@ -0,0 +1,132 @@
+<?php
+
+namespace App\Modules\SalesOrderManagement\Services;
+
+use App\Models\Order;
+use App\Models\TaxRegion;
+use App\Models\User;
+use App\Modules\SalesOrderManagement\Models\SalesQuotation;
+use App\Modules\SalesOrderManagement\Models\SalesQuotationItem;
+use Illuminate\Support\Facades\Auth;
+use Illuminate\Support\Facades\DB;
+use Illuminate\Support\Facades\Log;
+
+/**
+ * Keeps SOM's Sales Quotations in sync with the CRM's `orders` table.
+ *
+ * Whenever a customer places (or updates) an order on the CRM/e-commerce
+ * side, this service creates the matching quotation automatically — status
+ * mapped from the order's own status (pending -> Draft, processing -> Sent,
+ * shipped/delivered -> Accepted, cancelled -> Rejected) — or, if one already
+ * exists for that order, refreshes its line items, totals, and status. No
+ * more pressing "+ New Quotation" by hand.
+ *
+ * Orders that originated FROM a quotation (see SalesOrderController::
+ * acceptQuotation() + CustomerMockController::placeOrder(), which write the
+ * mirrored `orders` row after the fact) are skipped, otherwise every
+ * checkout would spawn a second, duplicate quotation for itself.
+ */
+class AutoQuotationService
+{
+    public function syncFromOrder(Order $order): ?SalesQuotation
+    {
+        $order->loadMissing('items.product');
+
+        // Order came *from* SOM already (quotation accepted -> sales order ->
+        // mirrored here). It already has a quotation behind it, so skip it.
+        if ($this->originatedFromQuotation($order)) {
+            return null;
+        }
+
+        if ($order->items->isEmpty()) {
+            // Items are usually inserted right after the order in the same
+            // request; nothing to price yet. Callers re-fire this on the
+            // order_items side too, so it'll catch up.
+            return null;
+        }
+
+        return DB::transaction(function () use ($order) {
+            $existing = SalesQuotation::where('source_order_id', $order->order_id)->first();
+
+            $subtotal = 0;
+            $discountTotal = 0;
+            $lines = [];
+
+            foreach ($order->items as $item) {
+                $unitPrice = (float) $item->unit_price;
+                $qty = (int) $item->quantity;
+                $lineGross = $unitPrice * $qty;
+
+                $subtotal += $lineGross;
+                $lines[] = [
+                    'product_id'       => $item->product_id,
+                    'quantity'         => $qty,
+                    'unit_price'       => $unitPrice,
+                    'discount_percent' => 0,
+                    'line_total'       => round($lineGross, 2),
+                ];
+            }
+
+            $taxRegion = TaxRegion::where('is_default', true)->first() ?? TaxRegion::firstOrFail();
+            $taxable = $subtotal - $discountTotal;
+            $tax = round($taxable * ((float) $taxRegion->vat_rate / 100), 2);
+
+            $attrs = [
+                'customer_id'      => $order->customer_id,
+                'tax_region_id'    => $taxRegion->id,
+                'quotation_date'   => now()->toDateString(),
+                'valid_until'      => now()->addDays(15)->toDateString(),
+                'status'           => $this->mapOrderStatus($order->status),
+                'subtotal'         => round($subtotal, 2),
+                'discount_amount'  => round($discountTotal, 2),
+                'tax_amount'       => $tax,
+                'total_amount'     => round($taxable + $tax, 2),
+                'source_order_id'  => $order->order_id,
+            ];
+
+            if ($existing) {
+                $existing->update($attrs);
+                $existing->items()->delete();
+                $quotation = $existing;
+            } else {
+                $quotation = SalesQuotation::create($attrs + [
+                    'created_by' => Auth::id() ?? User::query()->value('id'),
+                ]);
+            }
+
+            foreach ($lines as $line) {
+                SalesQuotationItem::create($line + ['quotation_id' => $quotation->quotation_id]);
+            }
+
+            Log::info('AutoQuotationService: synced quotation from CRM order', [
+                'order_id'      => $order->order_id,
+                'quotation_id'  => $quotation->quotation_id,
+            ]);
+
+            return $quotation;
+        });
+    }
+
+    protected function originatedFromQuotation(Order $order): bool
+    {
+        // CustomerMockController::placeOrder() stamps order_number with the
+        // SOM sales order id it just created (e.g. "SO-0001").
+        return is_string($order->order_number) && str_starts_with($order->order_number, 'SO-');
+    }
+
+    /**
+     * Maps the CRM's order.status (pending/processing/shipped/delivered/
+     * cancelled) to SOM's quotation status (Draft/Sent/Accepted/Rejected),
+     * so a quotation synced from an already-fulfilled order doesn't sit
+     * around looking like it's still awaiting action.
+     */
+    protected function mapOrderStatus(?string $orderStatus): string
+    {
+        return match (strtolower($orderStatus ?? '')) {
+            'processing'          => 'Sent',
+            'shipped', 'delivered' => 'Accepted',
+            'cancelled'           => 'Rejected',
+            default               => 'Draft', // pending / unknown
+        };
+    }
+}
diff --git a/app/Modules/SalesPerformanceReporting/Models/UserSetting.php b/app/Modules/SalesPerformanceReporting/Models/UserSetting.php
index 3a8d6e2..cb59fd0 100644
--- a/app/Modules/SalesPerformanceReporting/Models/UserSetting.php
+++ b/app/Modules/SalesPerformanceReporting/Models/UserSetting.php
@@ -6,13 +6,6 @@
 use Illuminate\Database\Eloquent\Model;
 use Illuminate\Database\Eloquent\Relations\BelongsTo;
 
-/**
- * Backs sales_performance_reporting_user_settings. The table existed since
- * the original migration but had no model — AppServiceProvider's view
- * composer referenced a `settings` relation that couldn't resolve to
- * anything. This just makes that relation real; there's still no
- * Settings UI reading/writing these columns yet.
- */
 class UserSetting extends Model
 {
     protected $table = 'sales_performance_reporting_user_settings';
@@ -27,13 +20,13 @@ class UserSetting extends Model
     ];
 
     protected $casts = [
-        'notifications_enabled' => 'boolean',
-        'dark_mode_enabled' => 'boolean',
+        'notifications_enabled'   => 'boolean',
+        'dark_mode_enabled'       => 'boolean',
         'quota_reminders_enabled' => 'boolean',
     ];
 
     public function user(): BelongsTo
     {
-        return $this->belongsTo(User::class);
+        return $this->belongsTo(User::class, 'user_id');
     }
 }
diff --git a/database/migrations/2026_07_26_000001_add_source_order_id_to_sales_quotations_table.php b/database/migrations/2026_07_26_000001_add_source_order_id_to_sales_quotations_table.php
new file mode 100644
index 0000000..370fb5e
--- /dev/null
+++ b/database/migrations/2026_07_26_000001_add_source_order_id_to_sales_quotations_table.php
@@ -0,0 +1,31 @@
+<?php
+
+use Illuminate\Database\Migrations\Migration;
+use Illuminate\Database\Schema\Blueprint;
+use Illuminate\Support\Facades\Schema;
+
+/**
+ * Links a Sales Quotation back to the CRM/e-commerce `orders` row that
+ * auto-generated it (see AutoQuotationService). Null for quotations a
+ * sales rep still builds by hand.
+ */
+return new class extends Migration
+{
+    public function up(): void
+    {
+        Schema::table('sales_order_management_sales_quotations', function (Blueprint $table) {
+            $table->unsignedBigInteger('source_order_id')->nullable()->after('customer_id');
+            $table->index('source_order_id');
+            $table->foreign('source_order_id')->references('order_id')->on('orders')->nullOnDelete();
+        });
+    }
+
+    public function down(): void
+    {
+        Schema::table('sales_order_management_sales_quotations', function (Blueprint $table) {
+            $table->dropForeign(['source_order_id']);
+            $table->dropIndex(['source_order_id']);
+            $table->dropColumn('source_order_id');
+        });
+    }
+};
diff --git a/database/seeders/OrderSeeder.php b/database/seeders/OrderSeeder.php
index 31d4470..a7f02f8 100644
--- a/database/seeders/OrderSeeder.php
+++ b/database/seeders/OrderSeeder.php
@@ -126,6 +126,27 @@ public function run(): void
         });
 
         $this->refreshCustomerInsights();
+        $this->syncQuotationsFromSeededOrders();
+    }
+
+    /**
+     * OrderObserver/OrderItemObserver *should* auto-generate a quotation
+     * for each order as it's created above, but relying on model events
+     * firing correctly through hundreds of rapid-fire creates in a single
+     * seeder run is fragile. Doing an explicit pass here guarantees every
+     * seeded order ends up with a matching Sales Quotation, regardless of
+     * event timing — same result you'd get from manually running
+     * `php artisan quotations:sync-from-orders` afterward, just automatic.
+     */
+    private function syncQuotationsFromSeededOrders(): void
+    {
+        $service = app(\App\Modules\SalesOrderManagement\Services\AutoQuotationService::class);
+
+        Order::with('items')->chunkById(200, function ($orders) use ($service) {
+            foreach ($orders as $order) {
+                $service->syncFromOrder($order);
+            }
+        });
     }
 
     /**
diff --git a/resources/views/sales-order-management/sales-order-management.css b/resources/views/sales-order-management/sales-order-management.css
index 6e8899a..d375015 100644
--- a/resources/views/sales-order-management/sales-order-management.css
+++ b/resources/views/sales-order-management/sales-order-management.css
@@ -1,7 +1,7 @@
 @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');
   :root{
     /* ── Palette tokens (from Color Palette spec) ── */
-    --background:#F3F4FD;--secondary:#B0B4EC;--accent:#120F34;
+    --background:#E9EBFC;--secondary:#B0B4EC;--accent:#120F34;
     --primary:#CFD2F9;--primary-light:#E9EBFC;
     --header-boxes:#D7D7D7;--sidebar-buttons:#C3C3C3;
     --vip:#AD9EFF;--rb:#9CFF9F;--nc:#7ED8FF;--risk:#FF9A91;
@@ -9,15 +9,18 @@
     --ar:#EA3030;
     --indicator-yellow:#F9DFAA;--indicator-text-yellow:#CC9B28;
 
-    /* ── Existing aliases mapped onto the palette above (kept so the rest of the CSS keeps working) ── */
-    --c0:var(--background);--c1:var(--primary-light);--c2:var(--primary);--c3:var(--header-boxes);
-    --line:var(--sidebar-buttons);--red:var(--accent);--red-dim:var(--risk);
+    /* ── Existing aliases, now matched to the app-wide "curema" tokens
+       (see resources/views/layouts/app.blade.php Tailwind config) so this
+       module's cards/borders/inputs read as the same product, not a
+       different grey-toned skin. ── */
+    --c0:var(--background);--c1:var(--primary-light);--c2:var(--primary);--c3:var(--primary-light);
+    --line:var(--primary);--red:var(--accent);--red-dim:var(--risk);
     --amber:#5B3FA8;--amber-bg:var(--vip);
     --green:var(--text-green);--green-bg:var(--rb);
     --blue:var(--btext-blue);--blue-bg:var(--nc);
     --purple:var(--textt-green);
     --alert:var(--ar);--alert-bg:var(--risk);
-    --t0:var(--accent);--t1:#4D4A68;--t2:#8884A3;
+    --t0:var(--accent);--t1:#3A3760;--t2:#5B5876;
   }
   *{box-sizing:border-box;}
   html,body{margin:0;padding:0;background:var(--c0);color:var(--t0);font-family:'Inter',sans-serif;}
@@ -52,10 +55,10 @@
   .modnav-item.open .chev{transform:rotate(180deg);}
   .modnav-item.disabled-mod{opacity:.55;}
 
-  .modnav-drop{display:none;position:absolute;top:100%;left:50%;transform:translateX(-50%);min-width:260px;background:#fff;border:1px solid var(--line);border-top:none;border-radius:0 0 8px 8px;box-shadow:0 12px 28px rgba(18,15,52,.16);padding:8px;text-align:left;}
+  .modnav-drop{display:none;position:absolute;top:100%;left:50%;transform:translateX(-50%);min-width:260px;background:#fff;border:1px solid var(--line);border-top:none;border-radius:0 0 16px 16px;box-shadow:0 12px 28px rgba(18,15,52,.16);padding:8px;text-align:left;}
   .modnav-item.open .modnav-drop{display:block;}
   .modnav-drop-hd{font-family:'Inter',sans-serif;font-size:8.5px;color:var(--t2);letter-spacing:1.5px;text-transform:uppercase;padding:8px 10px 5px;}
-  .modnav-drop-item{display:flex;align-items:center;gap:10px;padding:9px 11px;border-radius:4px;font-family:'Inter',sans-serif;font-size:13px;font-weight:600;color:var(--t1);cursor:pointer;white-space:nowrap;}
+  .modnav-drop-item{display:flex;align-items:center;gap:10px;padding:9px 11px;border-radius:12px;font-family:'Inter',sans-serif;font-size:13px;font-weight:600;color:var(--t1);cursor:pointer;white-space:nowrap;}
   .modnav-drop-item .ico{font-size:13px;width:16px;text-align:center;opacity:.75;flex-shrink:0;}
   .modnav-drop-item:hover{background:var(--c2);color:var(--t0);}
   .modnav-drop-item.active{background:var(--c2);color:var(--red);}
@@ -84,12 +87,12 @@
 
   /* STATS */
   .stats{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-bottom:18px;}
-  .stat{background:#fff;border:1px solid var(--line);border-radius:5px;padding:13px 15px;position:relative;overflow:hidden;box-shadow:0 1px 2px rgba(18,15,52,.04);}
+  .stat{background:#fff;border:1px solid var(--line);border-radius:16px;padding:13px 15px;position:relative;overflow:hidden;box-shadow:0 1px 2px rgba(18,15,52,.04);}
   .stat::before{content:'';position:absolute;left:0;top:0;bottom:0;width:3px;background:var(--acc,var(--red));}
-  .stat-lbl{font-family:'Inter',sans-serif;font-size:9px;color:var(--t2);letter-spacing:1.5px;text-transform:uppercase;}
+  .stat-lbl{font-family:'Inter',sans-serif;font-size:12px;color:var(--t2);}
   .stat-val{font-family:'Inter',sans-serif;font-size:24px;font-weight:600;margin-top:3px;}
   .sync-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:16px;}
-  .sync-card{background:#fff;border:1px solid var(--line);border-radius:6px;padding:16px 18px;border-left:3px solid var(--acc,var(--red));box-shadow:0 1px 2px rgba(18,15,52,.04);}
+  .sync-card{background:#fff;border:1px solid var(--line);border-radius:16px;padding:16px 18px;border-left:3px solid var(--acc,var(--red));box-shadow:0 1px 2px rgba(18,15,52,.04);}
   .sync-card-top{display:flex;align-items:center;justify-content:space-between;margin-bottom:14px;}
   .sync-card-top h3{margin:0;font-family:'Inter',sans-serif;font-size:16px;font-weight:600;color:var(--t0);}
   .badge-sync{font-family:'Inter',sans-serif;font-size:9px;letter-spacing:.6px;background:var(--nc);color:var(--btext-blue);padding:3px 8px;border-radius:20px;font-weight:600;}
@@ -100,7 +103,7 @@
   .sync-card-actions{display:flex;gap:8px;}
 
   /* PANEL */
-  .panel{background:#fff;border:1px solid var(--line);border-radius:6px;margin-bottom:16px;box-shadow:0 1px 2px rgba(18,15,52,.04);overflow:hidden;}
+  .panel{background:#fff;border:1px solid var(--line);border-radius:16px;margin-bottom:16px;box-shadow:0 1px 2px rgba(18,15,52,.04);overflow:hidden;}
   .panel-head,.panel-hd{display:flex;align-items:center;justify-content:space-between;padding:13px 17px;border-bottom:1px solid var(--line);gap:12px;}
   .panel-hd > span:only-child{width:100%;text-align:center;}
   .panel-head h2{font-family:'Inter',sans-serif;font-size:13.5px;font-weight:600;margin:0;letter-spacing:.3px;}
@@ -112,14 +115,14 @@
   .panel table tbody tr:last-child td{border-bottom:none;}
 
   /* SEARCH INPUT (used in panel-hd toolbars) */
-  .search{font-family:'Inter',sans-serif;background:var(--c3);border:1px solid var(--line);color:var(--t0);padding:7px 10px;border-radius:3px;font-size:13px;font-weight:500;flex:1;min-width:180px;max-width:320px;}
+  .search{font-family:'Inter',sans-serif;background:var(--c3);border:1px solid var(--line);color:var(--t0);padding:7px 10px;border-radius:12px;font-size:13px;font-weight:500;flex:1;min-width:180px;max-width:320px;}
   .search:focus{outline:none;border-color:var(--red);}
 
   /* DETAIL VIEW (quotation/order detail panel) */
   .panel.detail{padding:20px 22px;}
 
   /* ORDER RECEIPT VIEW (customer invoice-style detail) */
-  .receipt-head{background:#fff;border:1px solid var(--line);border-radius:6px;padding:18px 22px;margin-bottom:16px;box-shadow:0 1px 2px rgba(18,15,52,.04);}
+  .receipt-head{background:#fff;border:1px solid var(--line);border-radius:16px;padding:18px 22px;margin-bottom:16px;box-shadow:0 1px 2px rgba(18,15,52,.04);}
   .receipt-head h1{font-family:'Inter',sans-serif;font-size:22px;letter-spacing:.5px;margin:0 0 4px;}
   .receipt-addr{font-family:'Inter',sans-serif;font-size:13px;color:var(--t1);margin-bottom:6px;}
   .receipt-sub{font-family:'Inter',sans-serif;font-size:10.5px;color:var(--t2);letter-spacing:.3px;}
@@ -160,11 +163,11 @@
 
   /* MODULE LINK PANEL */
   .mod-link-row{display:flex;gap:12px;margin-bottom:16px;}
-  .mod-link{flex:1;border:1px solid var(--line);border-radius:4px;padding:14px 16px;display:flex;flex-direction:column;gap:6px;position:relative;overflow:hidden;}
+  .mod-link{flex:1;border:1px solid var(--line);border-radius:12px;padding:14px 16px;display:flex;flex-direction:column;gap:6px;position:relative;overflow:hidden;}
   .mod-link::before{content:'';position:absolute;left:0;top:0;bottom:0;width:3px;background:var(--ml-acc,var(--blue));}
   .mod-link-header{display:flex;align-items:center;justify-content:space-between;}
   .mod-link-title{font-family:'Inter',sans-serif;font-size:12.5px;font-weight:600;letter-spacing:.3px;color:var(--t0);}
-  .mod-link-badge{font-family:'Inter',sans-serif;font-size:9px;letter-spacing:.8px;text-transform:uppercase;padding:2px 7px;border-radius:2px;}
+  .mod-link-badge{font-family:'Inter',sans-serif;font-size:9px;letter-spacing:.8px;text-transform:uppercase;padding:2px 7px;border-radius:999px;}
   .mod-link-desc{font-family:'Inter',sans-serif;font-size:9.5px;color:var(--t2);line-height:1.5;}
   .mod-link-data{display:flex;gap:14px;margin-top:4px;}
   .mod-link-kv{display:flex;flex-direction:column;}
@@ -173,7 +176,7 @@
   .mod-link-actions{display:flex;gap:8px;margin-top:8px;flex-wrap:wrap;}
 
   /* BUTTONS */
-  .btn{font-family:'Inter',sans-serif;font-weight:600;font-size:12.5px;letter-spacing:.4px;border:1px solid var(--line);background:var(--c2);color:var(--t0);padding:7px 13px;border-radius:3px;cursor:pointer;transition:.15s;}
+  .btn{font-family:'Inter',sans-serif;font-weight:600;font-size:12.5px;letter-spacing:.4px;border:1px solid var(--line);background:var(--c2);color:var(--t0);padding:7px 13px;border-radius:12px;cursor:pointer;transition:.15s;}
   .btn:hover{border-color:var(--t2);}
   .btn.primary{background:var(--red);border-color:var(--red);color:#fff;}
   .btn.primary:hover{background:#2C2860;}
@@ -187,13 +190,13 @@
 
   /* TABLE */
   table{width:100%;border-collapse:collapse;}
-  th{text-align:left;font-family:'Inter',sans-serif;font-size:9.5px;color:var(--t2);letter-spacing:1px;text-transform:uppercase;padding:7px 10px;border-bottom:1px solid var(--line);white-space:nowrap;}
+  th{text-align:left;font-family:'Inter',sans-serif;font-size:12px;font-weight:500;color:var(--t2);padding:9px 10px;border-bottom:1px solid var(--line);white-space:nowrap;}
   td{padding:10px 10px;border-bottom:1px solid var(--c2);font-size:13px;vertical-align:middle;}
   tr.row:hover{background:var(--c2);cursor:pointer;}
   .mono{font-family:'Inter',sans-serif;font-size:11.5px;color:var(--t1);}
 
   /* BADGES */
-  .badge{display:inline-flex;align-items:center;gap:5px;font-family:'Inter',sans-serif;font-size:9.5px;letter-spacing:.5px;text-transform:uppercase;padding:3px 8px;border-radius:20px;font-weight:600;white-space:nowrap;}
+  .badge{display:inline-flex;align-items:center;gap:5px;font-family:'Inter',sans-serif;font-size:12px;padding:4px 10px;border-radius:999px;font-weight:500;white-space:nowrap;}
   .dot{width:5px;height:5px;border-radius:50%;flex-shrink:0;}
   .b-quote{background:var(--amber-bg);color:var(--amber);}.b-quote .dot{background:var(--amber);}
   .b-confirmed{background:#A8E6E0;color:#0B7A6F;}.b-confirmed .dot{background:#0B7A6F;}
@@ -229,7 +232,7 @@
   .b-Inactive{background:#E5E5E5;color:#555;}.b-Inactive .dot{background:#555;}
 
   /* FORMS */
-  input,select{font-family:'Inter',sans-serif;background:var(--c3);border:1px solid var(--line);color:var(--t0);padding:7px 10px;border-radius:3px;font-size:13px;font-weight:500;width:100%;}
+  input,select{font-family:'Inter',sans-serif;background:var(--c3);border:1px solid var(--line);color:var(--t0);padding:7px 10px;border-radius:12px;font-size:13px;font-weight:500;width:100%;}
   input:focus,select:focus{outline:none;border-color:var(--red);}
   label{font-size:9.5px;color:var(--t2);font-family:'Inter',sans-serif;letter-spacing:.8px;text-transform:uppercase;display:block;margin-bottom:5px;}
   .field{margin-bottom:13px;}
@@ -251,7 +254,7 @@
 
   /* MODAL */
   .modal-bg{position:fixed;inset:0;background:rgba(0,0,0,.65);display:flex;align-items:center;justify-content:center;z-index:50;}
-  .modal{background:var(--c1);border:1px solid var(--line);border-radius:6px;width:580px;max-width:92vw;max-height:88vh;overflow-y:auto;}
+  .modal{background:var(--c1);border:1px solid var(--line);border-radius:16px;width:580px;max-width:92vw;max-height:88vh;overflow-y:auto;}
   .modal-head{padding:14px 20px;border-bottom:1px solid var(--line);display:flex;justify-content:space-between;align-items:center;}
   .modal-head h3{margin:0;font-family:'Inter',sans-serif;font-size:15px;letter-spacing:.3px;}
   .modal-body{padding:20px;}
@@ -265,10 +268,10 @@
   .t-row.grand .mono{color:var(--red);font-size:16px;}
 
   /* TOAST */
-  .toast{position:fixed;bottom:24px;right:24px;background:var(--c2);border:1px solid var(--line);border-radius:4px;padding:10px 16px;font-family:'Inter',sans-serif;font-size:11px;color:var(--t0);z-index:99;display:flex;align-items:center;gap:8px;box-shadow:0 4px 16px rgba(0,0,0,.4);animation:slideIn .2s ease;}
+  .toast{position:fixed;bottom:24px;right:24px;background:var(--c2);border:1px solid var(--line);border-radius:12px;padding:10px 16px;font-family:'Inter',sans-serif;font-size:11px;color:var(--t0);z-index:99;display:flex;align-items:center;gap:8px;box-shadow:0 4px 16px rgba(0,0,0,.4);animation:slideIn .2s ease;}
   .toast .toast-ico{font-size:14px;}
   @keyframes slideIn{from{transform:translateY(10px);opacity:0}to{transform:translateY(0);opacity:1}}
 
   ::-webkit-scrollbar{width:7px;height:7px;}
-  ::-webkit-scrollbar-thumb{background:var(--c3);border-radius:4px;}
+  ::-webkit-scrollbar-thumb{background:var(--c3);border-radius:12px;}
   ::-webkit-scrollbar-track{background:var(--c1);}
\ No newline at end of file
diff --git a/resources/views/sales-order-management/sales-order-management.js b/resources/views/sales-order-management/sales-order-management.js
index 1d0792d..880f565 100644
--- a/resources/views/sales-order-management/sales-order-management.js
+++ b/resources/views/sales-order-management/sales-order-management.js
@@ -211,7 +211,7 @@ function QuotationsTab() {
   <div class="panel">
     <div class="panel-hd">
       <input id="searchBox" class="search" placeholder="Search quotations…" value="${state.filterText}">
-      <button class="btn primary" id="newQuoteBtn">+ New Quotation</button>
+      <span class="hint" title="Quotations are created automatically when a customer places an order in the CRM.">🔄 Synced from CRM orders</span>
     </div>
     <table>
       <thead><tr><th>Quotation</th><th>Customer</th><th>Date</th><th>Valid Until</th><th>Total</th><th>Status</th><th></th></tr></thead>
-- 
2.43.0

